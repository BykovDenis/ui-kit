interface IPalette {
  baseButtonFontColor: string;
  baseFontColor: string;
  primary: {
    light: string,
    lighter: string,
    main: string,
  };
  secondary: {
    light: string,
    lighter: string,
    main: string,
  };
}

export default IPalette;
